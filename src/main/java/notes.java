

/*
public enum Professions.Profession {
    Professions.Duelist("A duelist"),
    Bruiser("A Bruiser"),
    Knight("A Knight"),
    Kabal("A Kabal"),
    Trickster("A Trickster");

    final String name;

    Professions.Profession(String name) {
        this.name = name;
    }

    public boolean include(String other) {
        Professions.Profession[] classTypes = Professions.Profession.values();
        for (Professions.Profession classType : classTypes) {
            if(classType.name.equals(other)) {
                return true;
            }
        }
        return false;
    }
    public boolean isDuelist(String name) {
        return this.name.equals(name);
    }

    public void Stuff(String name) {

    }
}

    public boolean include(String string) {
        ClassTypes[] classType = ClassTypes.values();
        for (ClassType aFruit : ClassTypes)
            if (aFruit.fruitname.equals(aName))
                return true;
        return false;
    static void test(ClassTypes classType) {
        // Switch on the enum value.
        switch (classType) {
            case Professions.Duelist:
                System.out.println("?");
                return;
            case Bruiser:
                System.out.println("Is medium size");
                return;
            case Knight:
                System.out.println("!");
                return;
            case Kabal:
                System.out.println("!");
                return;
            case Trickster:
                System.out.println("!");
                return;
        }
    }

import java.lang.Enum;



    ClassType



    }
}
}
*/